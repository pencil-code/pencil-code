#!/bin/sh
# Wrapper to overwrite var.dat with var.dat.bck

if [ -n "$1" ]; then
    d="$1"
    suffix=".bck.$d"
else
    suffix=".bck"
fi
src="var.dat$suffix"
dst="var.dat"
pc_move_var "$src" "$dst"


#!/bin/csh
###                       lnsrc
###                      -------
###  Link files into a local src directory

if (-d ./src) then
  echo "src already exists"
else
  mkdir ./src
endif

cd src
#
ln -s ../../../src/*.f90 .
ln -s ../../../src/Makefile .
ln -s ../../../src/*.c .


# host-Acamar.local-Darwin.conf
#
# Settings for my laptop at the Lund Observatory, Sweden
#
# $Id$

%include os/Darwin
%include mpi/default

%section runtime
    mpiexec_opts = --use-hwthread-cpus --mca shmem posix
%endsection runtime

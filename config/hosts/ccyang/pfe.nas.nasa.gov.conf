# Settings for the NASA Pleiades system.
#
# $Id$

%include compilers/Intel_MPI
%include compilers/extensions/Intel_optimal

%section Makefile
	FFLAGS += -ip -mp1 -w -vec-report0 -opt-report0
	CFLAGS += -ip -mp1 -w -vec-report0 -opt-report0
%endsection Makefile

